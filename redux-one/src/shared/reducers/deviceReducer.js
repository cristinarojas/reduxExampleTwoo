// 3) CREATE THE REDUCER.
// the reducers functions receive a state and action and return a new state.
// this reducer receive a state.
export default function deviceReducer(state = {}) {
  return state;
}
